package Objects;

import Moving.Player;

public interface Equipable {

    ////////////////////////////////////////////////////////////////////////////////////////<diverseMethods>

    boolean equip(Player p);

    void unequip(Player p);
}
